# UnityClient 模块化迁移计划

> 本文件只保存稳定路线、依赖关系和完成门禁。
> 当前完成率、当前批次和最新验证只看 `UNITYCLIENT_STATUS.md`。
> 工程：`E:\neiwang_kapai\Game\unityclient`。

## 1. 目标与原则

保持原 C++ 游戏服协议和 Lua 业务意图，以 Unity/uGUI + C# 通用基础设施 + xLua 业务控制器重建客户端。

```text
运行时底座 → 网络/协议 → 数据/配置 → UI 通用层 → 资源/音频 → 自动化
→ 登录与主界面 → 基础业务 → 成长业务 → 社交/玩法 → 活动/商业化 → 发布
```

原则：

- 不逐字翻译旧 `cc.*`、`LUIBase` 和旧消息总线。
- 不一次性搬运全部旧 Lua。
- 静态 Prefab 数量不等于功能完成率。
- 每个协议必须有服务端、旧客户端、真实回包三方证据。
- 每个模块必须经过 Store，UI 不直接处理原始包。
- 消耗、领奖、培养等变更使用隔离角色。
- 本地测试旁路必须受 `local_test=1` 保护。

## 2. 分层边界

| 层 | 职责 | 禁止事项 |
|---|---|---|
| Core | App 生命周期、服务容器、环境和状态机 | 业务协议散落在 MonoBehaviour |
| LuaRuntime | 模块加载、错误边界、受控重载 | 全量搬运 Cocos Lua |
| Network | TCP、包头、队列、超时、重连 | Presenter 直接写 Socket |
| Protocol | 协议号、读写器、分发、请求上下文 | 根据注释猜字段 |
| Data | 配置、Store、增量合并、通用模型 | UI 持有原始字节流 |
| UI Foundation | Router、层级、列表、弹窗、红点、Loading | 每个模块复制框架 |
| Presenter | 节点绑定、渲染、交互转发 | 写业务奖励和数值规则 |
| Lua Business | 协议业务和按钮流程 | 直接调用 `cc.*` |
| Validation | 静态、协议、PlayMode、截图和人工 QA | 只看旧 COMPLETE 判通过 |

## 3. 阶段 A：底层与质量基线

### A0 工程与生命周期

- Bootstrap 固定为 Build Index 0，并可通过 Editor 脚本重建。
- Local/Dev/Release 配置分层，命令行由 AppLaunchOptions 集中解析。
- Dispose 顺序固定为 UI → Lua → Protocol → Network。
- 文档/静态修改不启动重进程；验证后关闭本阶段进程。

### A1 网络与协议

- 保留旧包头、UTF-16LE、零载荷包、每 socket send queue。
- ProtocolRegistry 记录协议号、模块、方向、op 和解析器。
- RequestContext 记录发送时间、超时和当前模块。
- 未知协议、越界、超时、断线发包必须产生可判定错误。
- 维护“服务端实现—旧客户端—Unity 覆盖”矩阵。

### A2 数据与配置

- 配置按需加载、缓存、校验缺表/重复 ID。
- 玩家状态进入独立 Store，增量只修改 Store。
- Reward、Item、Attribute、Currency 建立通用模型。
- 活动和刷新倒计时只使用 ServerTime。
- 切号清角色 Store，不误清配置缓存。

### A3 UI 与资源

- UI 层级：Scene、Main、Window、Popup、Guide、Toast、Loading。
- Router/UiStack 统一打开、关闭、Esc、遮罩和返回主界面。
- 复用 VirtualList、MessageBox、Toast、Loading、Reward、货币栏。
- ResourceService 统一缓存、回退、释放、占位和缺图统计。
- 手工 Prefab 默认只读，以运行时绑定恢复行为。

### A4 自动化

每个模块至少覆盖：

1. Prefab/路径/资源静态校验。
2. Reader/Writer 字段边界。
3. 空、正常、增量、错误数据。
4. 隔离角色真实协议 PlayMode。
5. Esc、重复打开、断线、切号。
6. 编译与严重异常扫描。
7. 固定分辨率 GameView 截图。
8. 结果新鲜度和进程清理。

## 4. 阶段 B：基础流程与主界面

顺序：

1. 启动与环境配置。
2. 登录、创角、选角、断线与重连。
3. 主界面 HUD、货币、体力、战力。
4. 系统设置、公告、切换账号。
5. 主入口注册、功能开启和红点树。

正式登录服源码缺失时只保留本地直连开关，不伪造线上账号协议。

## 5. 阶段 C：基础业务与成长

### C1 背包、任务与奖励

顺序：背包全量 → 增量 → 使用/合成/出售 → 任务列表 → 增量/追踪 → 领奖 → 奖励统一表现。

依赖：Player/Currency、ItemCatalog、ResourceService、RewardStore、VirtualList、Toast。

### C2 神将与阵容

顺序：神将列表 → 详情/属性 → 阵容站位 → 上下阵 → 升级/进阶 → 技能 → 图鉴。

协议按 `/24、/25、/40、/41、/48、/51、/69、/70、/322` 逐项取证。

### C3 装备、法宝与生产链

顺序：装备列表/槽位 → 穿脱 → 强化 → 精炼/觉醒/神铸 → 合成 → 分解/回收 → 法宝/神器。

培养和回收属于不可逆操作，必须展示消耗预览并使用隔离角色。

### C4 邮件

顺序：列表 → 已读 → 附件 → 单封领取 → 一键领取 → 删除 → 红点/空态。

邮件复用 VirtualList、Reward、MessageBox、ServerTime 和红点树。

### C5 商城

顺序：商品列表 → 限购/刷新时间 → 货币 → 购买确认 → 单次购买 → 手动刷新 → 特殊商店。

充值、直购、渠道 SDK 不与基础商城混做，放入商业化阶段。

## 6. 阶段 D：社交、组织、世界与战斗

| 模块组 | 顺序 | 主要依赖 |
|---|---|---|
| 好友 | 列表→申请→添加→同意/拒绝→删除→赠送 | Player、VirtualList、Toast |
| 聊天 | 频道→私聊→历史→未读→屏蔽 | 好友、消息队列、敏感词 |
| 队伍 | 创建→邀请→成员→快捷组队 | 好友、场景状态 |
| 帮派 | 创建/申请→成员→捐献→科技→活动 | 社交、排行、奖励 |
| 世界 | 地图→NPC→跳转→任务追踪 | Loading、资源、任务 |
| 战斗 | 入战→过程→结算→回放 | 角色、技能、动画、音频 |
| 副本 | 关卡→消耗→战斗→结算→扫荡 | 战斗、奖励、体力 |

战斗不是第一个业务模块；角色、技能、资源和场景状态稳定后再进入。

## 7. 阶段 E：玩法、活动与商业化

| 模块组 | 内容 | 优先级 |
|---|---|---:|
| 深度成长 | 坐骑、翅膀、境界、神器 | P2 |
| 中型玩法 | 血战、塔、游历、列传、昆仑 | P2 |
| 福利 | 签到、首奖、阶段目标、在线奖励 | P2 |
| 抽卡 | 普通抽卡、活动抽卡、结果表现 | P2 |
| 活动 | 日常、限时、排行活动 | P3 |
| 商业化 | 充值、VIP、订单、直购 | P3 |
| 引导 | 新手引导、变强、功能开启 | 核心流程稳定后 |

活动与商业化依赖服务器时间、红点、奖励、商城和渠道 SDK，统一后置。

## 8. 阶段 F：发布

- Windows Development Player：启动、日志、窗口、资源路径。
- Android IL2CPP/ARM64：xLua AOT、裁剪、权限、包体、真机性能。
- 热更：版本清单、下载、校验、回滚；无正式方案时不伪造线上热更。
- SDK：账号、支付、隐私、实名、防沉迷、统计、崩溃上报。
- 性能：启动、主界面内存、列表、战斗帧率、GC、资源泄漏。
- 合规：日志脱敏、隐私授权、支付回调、第三方许可证。

## 9. 模块完成定义

一个模块只有同时满足以下条件才可标记为“阶段完成”：

| 编号 | 门禁 |
|---:|---|
| 1 | 原入口、Prefab、Lua 意图、服务端处理函数完成取证 |
| 2 | 请求/回包字段有代码或真实回包证据 |
| 3 | 数据进入 Store，UI 不直接解析网络包 |
| 4 | 使用真实迁移 Prefab，Presenter 不承载业务规则 |
| 5 | 空/正常/增量/错误/断线/重复打开/Esc 有明确结果 |
| 6 | 独立自动化入口不继承旧结果 |
| 7 | 消耗和奖励使用隔离角色并核对前后状态 |
| 8 | 严重异常为零，资源缺失有统计 |
| 9 | 图形环境可用时完成截图与点击 QA |
| 10 | 更新 STATUS、模块文档和协议覆盖矩阵 |

## 10. 文档维护

- 实时进度：只改 `UNITYCLIENT_STATUS.md`。
- 当前操作规则：只改 `UNITYCLIENT_HANDOFF.md`。
- 稳定路线变化：才改本文件。
- 模块协议与验证证据：写入 `docs/unityclient/modules/`。
- 日期流水和旧全文：写入 `docs/unityclient/history/`。
